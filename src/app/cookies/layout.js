export const metadata = {
    title: "Cookie Policy",
    description: "Cookie Policy for Insane Duo. Understanding how we use cookies to improve your experience.",
};

export default function CookiesLayout({ children }) {
    return <>{children}</>;
}
